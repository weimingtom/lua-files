## `glue.ipcall(iterator<v1,...>) -> iterator<true,v1,...|false,error>` ##

**Wraps an iterator such that each iteration is wrapped in a pcall.**

**Uses**
  * use iterators that can break with an error.

**Examples**

```
TODO
```